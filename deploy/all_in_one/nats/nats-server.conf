debug = true
trace = true

# Client port of 4222 on all interfaces
port: 4222

# HTTP monitoring port
monitor_port: 8222

jetstream: enabled

jetstream {
    store_dir: /data/jetstream
    max_mem: 8G
    max_file: 10G
}

authorization {
  ADMIN = {
    publish = ">"
    subscribe = ">"
  }

  users = [
    {user: "natsadmin", password: $2a$11$g4o5u9EfDRhxQrfo7y2dxeyOKM1HR/X7wXJotnCm4m1travhBb/t2, permissions: $ADMIN}
  ]
}

# This is for clustering multiple servers together.
# server_name=nats-master
# cluster {
#   # It is recommended to set a cluster name
#   name: "nats_cluster"

#   # Route connections to be received on any interface on port 6222
#   port: 6222

#   # Routes are protected, so need to use them with --routes flag
#   # e.g. --routes=nats-route://ruser:cT20ApSv3c2sr3xtbg@otherdockerhost:6222
#   authorization {
#     user: ruser
#     password: cT20ApSv3c2sr3xtbg
#     timeout: 2
#   }

#   # Routes are actively solicited and connected to from this server.
#   # This Docker image has none by default, but you can pass a
#   # flag to the nats-server docker image to create one to an existing server.
#   routes = []
# }
