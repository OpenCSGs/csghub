test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

aliyun_staging:
  service: Aliyun
  access_key_id: <%= Rails.application.credentials.aliyun_oss.staging[:access_id] %>
  access_key_secret: <%= Rails.application.credentials.aliyun_oss.staging[:access_secret] %>
  bucket: <%= Rails.application.credentials.aliyun_oss.staging[:bucket_name] %>
  endpoint: <%= Rails.application.credentials.aliyun_oss.staging[:endpoint] %>
  # path prefix, default: /
  path: "active_storage"
  # Bucket public: true/false, default: true, for generate public/private URL.
  public: false

aliyun:
  service: Aliyun
  access_key_id: <%= Rails.application.credentials.aliyun_oss.production[:access_id] %>
  access_key_secret: <%= Rails.application.credentials.aliyun_oss.production[:access_secret] %>
  bucket: <%= Rails.application.credentials.aliyun_oss.production[:bucket_name] %>
  endpoint: <%= Rails.application.credentials.aliyun_oss.production[:endpoint] %>
  # path prefix, default: /
  path: "active_storage"
  # Bucket public: true/false, default: true, for generate public/private URL.
  public: false
