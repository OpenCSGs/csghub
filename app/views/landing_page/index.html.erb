<landing-page
  :is-logged-in="<%= logged_in? %>"
  :hot-models="<%= @hot_models %>"
  :hot-datasets="<%= @hot_datasets %>"
  :hot-spaces="<%= @hot_spaces %>"
  :customers="<%= @customers.to_json %>"
>
</landing-page>