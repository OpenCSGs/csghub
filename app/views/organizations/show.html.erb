<organization-detail
  :organization="<%= @organization.to_json %>"
  :members="<%= @members.to_json %>"
  :models="<%= @models %>"
  :datasets="<%= @datasets %>"
  :spaces="<%= @spaces %>"
  :admin="<%= @admin %>"
>
</organization-detail>
